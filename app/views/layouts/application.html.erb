<!DOCTYPE html>
<html>
<head>
  <title>GardenApp</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <link rel="stylesheet" href="vendor/assets/font-awesome/css/font-awesome.min.css">
  <%= csrf_meta_tags %>

  <link href='http://fonts.googleapis.com/css?family=Kite+One' rel='stylesheet' type='text/css'>
  <link href='http://fonts.googleapis.com/css?family=Source+Sans+Pro:400,200italic|Raleway+Dots|Titillium+Web' rel='stylesheet' type='text/css'>
  <link rel="stylesheet" href="vendor/assets/font-awesome/css/font-awesome.min.css">
  <link href="//netdna.bootstrapcdn.com/font-awesome/3.2.1/css/font-awesome.css" rel="stylesheet">
</head>
<body>

  <header class="top group">

    <h1 id="growy">EARLY WORM</h2>

      <%= flash[:errors] if flash[:errors] %>

      <nav class="login-buttons">

      	<% if !current_user.nil? %>
            <% add_notifications(current_user) %>
            <li>Current User: <%= current_user.name %></li>
            <li><%= button_to "Logout", session_url, method: :delete %></li>

        <% else %>

            <%= button_to "Sign In", new_session_url, method: :get %>
            <%= button_to "Join", new_user_url, method: :get %>

      	<% end %>

      </nav>

      	<% if !current_user.nil? %>

          <%= render "gardens/notifications" %>

      	<% end %>

  </header>

    <div class="page-body group">

    	<%= yield %>

    </div>

</body>
</html>
